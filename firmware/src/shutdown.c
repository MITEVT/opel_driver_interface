#include <stdio.h> 
#include "types.h"
#include "util.h"

// Waits until velocity is zero to turn off

ERROR_T ShutdownStep(INPUT_T *input, OUTPUT_T *output, STATE_T *state, MODE_REQUEST_T mode_request){
    return NULL;
}
